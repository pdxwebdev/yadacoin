"""Commands package for YadaCoin CLI.

This package contains namespaces for different command groups (e.g., config, status).
Each namespace is a subpackage containing specific command implementations.

Namespaces:
    config: Configuration management commands
"""
