# yadaaiagenttherapist — MCP server dependencies
#
# The YadaCoin node (Python 3.9) does NOT need these.
# Install in a separate Python 3.10+ environment for the MCP server only.
#
#   python3.10 -m venv .venv310
#   source .venv310/bin/activate
#   pip install -r requirements.txt
#   python mcp_server.py
#
# Or with uvx (no venv needed):
#   uvx fastmcp run mcp_server.py --transport streamable-http --port 8010
fastmcp>=2.0
