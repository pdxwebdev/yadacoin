# Plugin dependencies for plugins/yadaaiagent
#
# Install from the repo root:
#   pip install -r plugins/yadaaiagent/requirements.txt
#
# yadacoin-agent-auth is a local SDK in sdk/python (not on PyPI).
# The path below is relative to the repo root.
../../sdk/python
coincurve>=13.0
python-bitcoinlib>=0.12.0
