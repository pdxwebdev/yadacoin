# Plugin dependencies for plugins/yadaaiagent
#
# Install from the repo root:
#   pip install -r plugins/yadaaiagent/requirements.txt
#
# The [node] extra wires up YadaCoinNodeKelProvider for a locally running node.
yadacoin-agent-auth>=1.3.1
